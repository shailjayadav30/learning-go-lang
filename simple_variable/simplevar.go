package simplevariable

import "fmt"
const error bool=false
func Simplevalues() {
v,err:=	fmt.Println("shailja")
	const age int = 10
	// name := "shailja"
	// var no int = 2
	const (
		role  = "user"
		valid = true
	)

	fmt.Println(v,err,error)
	// println(age, no, name, role, valid)
	// print(age, no, name, role, valid)

}
